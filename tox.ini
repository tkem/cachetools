[tox]
envlist = py,docs,doctest,pyright,ruff,ruff-format

[testenv]
deps =
    pytest
    pytest-cov
commands =
    py.test --basetemp={envtmpdir} --cov=cachetools --cov-report term-missing {posargs}

[testenv:docs]
deps =
     sphinx
commands =
     sphinx-build -W -b html -d {envtmpdir}/doctrees docs {envtmpdir}/html

[testenv:doctest]
deps =
     sphinx
commands =
     sphinx-build -W -b doctest -d {envtmpdir}/doctrees docs {envtmpdir}/doctest

[testenv:pyright]
deps =
    pyright
commands =
    pyright {posargs}

[testenv:ruff]
deps =
    ruff
commands =
    ruff check {posargs}

[testenv:ruff-format]
deps =
    ruff
commands =
    ruff format --diff -q {posargs}
